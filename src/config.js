const csvConfig = {
    required_fields: ["timestamp"],
    filter_fields: [ "speedometer", "gyrometer"],
    labels: ["label1", "label2"],
    label_attributes: {
        "label1": [],
        "label2": [],
    }
}

const uploaderConfig = {
    max_file_size: 200
};

